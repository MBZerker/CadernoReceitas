plugins {
    id "com.android.application" version "8.7.3" apply false
}

allprojects {
    buildDir = file("${System.getenv("TEMP")}/caderno-receitas-java-gradle-build/${project.path.replace(':', '_')}")
}
